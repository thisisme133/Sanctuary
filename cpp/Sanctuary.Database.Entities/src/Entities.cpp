// Placeholder to ensure library compilation
// This file ensures that a .lib file is generated even though
// all entity classes are header-only.

namespace Sanctuary::Database::Entities {
    // Ensures .lib is created even with header-only classes
}
